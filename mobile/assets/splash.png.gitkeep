# PLACEHOLDER — reemplazar con splash.png real (1242x2436 PNG recomendado)
# Este archivo existe para que git trackee la carpeta assets/.
# Antes de publicar en EAS Build, reemplazá con el splash screen real.
